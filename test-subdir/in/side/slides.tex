%% -*- latex -*-
\newtoks\nicsroot\directlua{ tex.settoks("nicsroot", os.getenv("NICS_ROOT") or error("NICS_ROOT environment variable has to be set, use the Makefile")) }
\input{\the\nicsroot /src/nics-cached.tex}
\endofdump
\input{\the\nicsroot /src/nics-noncached.tex}

\hypersetup{pdftitle={subdir nics tests}}

\begin{document}

\begin{slide}{Nics subdir test}{Works even in a subdir}
  \begin{nicscolumn}
    \nicsexterncode{yaml}
    \begin{nicsextern}{code}
      foo: bar
      nics: [very, good]
    \end{nicsextern}
  \end{nicscolumn}
\end{slide}

\end{document}
